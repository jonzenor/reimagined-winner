<div class="w-full text-right">
    @if ($primaryLink) <a href="{{ $primaryLink }}" class="mx-4 btn btn-primary">{{ $primaryText }}</a> &nbsp; @endif
    @if ($secondaryLink) <a href="{{ $secondaryLink }}" class="btn">{{ $secondaryText }}</a> @endif
</div>
